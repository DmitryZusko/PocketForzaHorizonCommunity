import { baseTheme } from "../constants";

export const styles = {
  accordion: {
    m: baseTheme.spacing(7),
    maxWidth: "98%",
    pb: baseTheme.spacing(15),
  },

  title: {
    ml: baseTheme.spacing(5),
  },
};
