name: Deploy API

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup .Net
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 7.0.x

      - name: Install Dependencies
        run: dotnet restore ./PocketForzaHorizonCommunity.Back/PocketForzaHorizonCommunity.Back.API/PocketForzaHorizonCommunity.Back.API.csproj

      - name: build
        run: dotnet build ./PocketForzaHorizonCommunity.Back/PocketForzaHorizonCommunity.Back.API/PocketForzaHorizonCommunity.Back.API.csproj --configuration Release --no-restore
